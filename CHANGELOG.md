# vite-plugin-local-webfonts

## 0.3.0

### Minor Changes

- f20d1c8: Add a Font Awesome kit provider that downloads webfonts and icon CSS the same way Adobe Fonts kits are downloaded. Domain-restricted kits can pass `origin`, which is sent as `Origin` and `Referer` at build time.
