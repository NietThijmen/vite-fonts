---
"vite-plugin-local-webfonts": minor
---

Add a Font Awesome kit provider that downloads webfonts and icon CSS the same way Adobe Fonts kits are downloaded. Domain-restricted kits can pass `origin`, which is sent as `Origin` and `Referer` at build time.
